import { useState } from 'react'
import { useIsMobile } from '@/hooks/use-mobile'

export function WhatsAppFloat() {
  const isMobile = useIsMobile()

  // Componente removido - retorna null para não renderizar nada
  return null
}